import re

with open(r'c:\Users\yusef\.gemini\antigravity-ide\scratch\SEEN\SEEN\app\merchant\[slug]\staff\page.tsx', 'r', encoding='utf-8') as f:
    content = f.read()

# I will just write out the complete file content instead of complex regex, because it's only 300 lines.
